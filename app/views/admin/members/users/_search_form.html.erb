<script>
  $(function() {
    $('.ui.accordion')
      .accordion()
    ;
  });
</script>
<div class="ui styled fluid accordion">
  <div class="title">
    <i class="dropdown icon"></i>
    <div class="ui large label">
      <i class="search icon"></i> Search
    </div>
  </div>
  <div class="content">
    <%= search_form_for [:admin, q], :class => 'ui form' do |f| %>
        <h4 class="ui dividing header">Search Conditions</h4>

        <div class="field">

          <div class="two fields">
            <div class="field">
              <%= f.label :name_start, "Name" %>
              <%= f.search_field :name_start, :placeholder => 'Name' %>
            </div>
            <div class="field">
              <%= f.label :name_start, "Email" %>
              <%= f.search_field :email_cont, :placeholder => 'Email' %>
            </div>
          </div>
        </div>

        <%= f.submit :class => 'ui button' %>
    <% end %>
  </div>
</div>


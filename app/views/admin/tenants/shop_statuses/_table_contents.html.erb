<div class="table-responsive">
  <table class="table table-striped jambo_table bulk_action">
    <thead>
    <tr class="headings">
      <th>
        <input type="checkbox" id="check-all" class="flat">
      </th>
      <th class="column-title"><%= t('admin.shop_statuses.items.name') %></th>
      <th class="column-title no-link last" colspan="3"><span class="nobr"><%= t('admin.shop_statuses.items.controls') %></span>
      </th>
      <th class="bulk-actions" colspan="13">
        <a class="antoo" style="color:#fff; font-weight:500;"><%= t('admin.elements.buttons.bulc_action') %> ( <span class="action-cnt"> </span> ) <i class="fa fa-chevron-down"></i></a>
      </th>
    </tr>
    </thead>

    <tbody>
    <% shop_statuses.each do |shop_status| %>
        <tr class="even pointer">
          <td class="a-center">
            <input type="checkbox" class="flat" name="table_records">
          </td>
          <td><%= shop_status.name %></td>
          <td><%= set_button_show('', [:admin, :tenants, shop_status]) %></td>
          <td><%= set_button_edit('', edit_admin_tenants_shop_status_path(shop_status)) %></td>
          <td><%= set_button_delete('', [:admin, :tenants, shop_status]) %></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>
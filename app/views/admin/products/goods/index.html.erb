<% provide(:title, t('admin.goods.title')) %>

<div class="right_col" role="main">
  <div class="">

    <%= render :partial => 'admin/shared/page_title', :locals => {title: t('admin.goods.title'), description: t('admin.goods.description')} %>

    <div class="clearfix"></div>
    <div class="row">
      <div class="clearfix"></div>
      <div class="col-md-12 col-sm-12 col-xs-12">
        <%= render 'layouts/admin_menus/goods_sub_tab' %>

        <div class="x_panel">
          <%= render :partial => 'admin/shared/table_title', :locals => {title: t('admin.goods.title'), description: t('admin.goods.description')} %>
          <div class="x_content">

            <%= render 'pagination' %>
            <%= render :partial => 'table_contents', :locals => {goods: @goods} %>
            <%= render 'pagination' %>

            <br>
            <%= set_button_new(t('admin.goods.buttons.add'), new_admin_products_good_path) %>
            <%= set_button_csv(t('admin.goods.buttons.csv_export'), admin_products_goods_path(format: 'csv')) %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
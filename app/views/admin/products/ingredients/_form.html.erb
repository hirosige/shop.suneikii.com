<%= form_for([:admin, :products, @ingredient]) do |f| %>
  <% if @ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
      <% @ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <fieldset class="form-group">
      <label for="name">素材名称</label>
      <%= f.text_field :name, :class => 'form-control' ,:placeholder => '素材名称', autofocus: true %>
  </fieldset>
  <div class="actions">
    <%= f.submit "素材追加", :class => 'w120 form-control btn btn-info' %>
  </div>
<% end %>

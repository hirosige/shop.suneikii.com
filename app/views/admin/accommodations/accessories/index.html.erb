<% provide(:title, t('admin.accessories.title')) %>

<div class="right_col" role="main">
  <div class="">

    <%= render :partial => 'admin/shared/page_title', :locals => {title: t('admin.accessories.title'), description: t('admin.accessories.description')} %>

    <div class="clearfix"></div>
    <div class="row">
      <div class="clearfix"></div>
      <div class="col-md-12 col-sm-12 col-xs-12">
        <%= render 'layouts/admin_menus/accommodations_sub_tab' %>

        <div class="x_panel">
          <%= render :partial => 'admin/shared/table_title', :locals => {title: t('admin.accessories.title'), description: t('admin.accessories.description')} %>
          <div class="x_content">

            <%= render 'pagination' %>
            <%= render :partial => 'table_contents', :locals => {goods: @accessories} %>
            <%= render 'pagination' %>

            <br>
            <%= set_button_new(t('admin.accessories.buttons.add'), new_admin_accommodations_accessory_path) %>
            <%= set_button_csv(t('admin.accessories.buttons.csv_export'), admin_accommodations_accessories_path(format: 'csv')) %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>